# ROBO DA BET V16 — BACKTEST REPORT

## Status

**NOT EXECUTED AS REAL BETTING BACKTEST — INSUFFICIENT REAL PIT ODDS DATA.**

The V16 backtest engine remains available and retains V15 semantics:

- event-aware records;
- decimal odds validation;
- EV/edge separation;
- stake policy separation;
- event-cluster bootstrap;
- ROI = total profit / total stake;
- bankroll return reported separately;
- CLV only when a genuine closing price is supplied.

No real ROI, profit, CLV or drawdown is reported from the DEMO fixture.
