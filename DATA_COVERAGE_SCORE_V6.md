# DATA COVERAGE SCORE V6

Formula: weighted mean across the declared layers. Match layers are normalized by 7,570 canonical matches; player entity layer is normalized against a fixed 1,000-entity engineering reference solely to prevent zero/undefined scaling. This is a coverage indicator, not model performance.

BEFORE_SCORE = 30.13
AFTER_SCORE = 30.60
DELTA = 0.47

Player-match, lineup, injury, suspension, xG and Exact PIT remain zero.
