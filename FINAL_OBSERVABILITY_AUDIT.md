# FINAL OBSERVABILITY AUDIT

V25 exposes counters and latency aggregates for provider, pricing, decision and model paths plus feed/data-quality and position events.

Session lifecycle status is exposed through the V25 API and the observation runner state file.

Deployed Prometheus/Grafana runtime was not executed because Docker/infrastructure was unavailable.
