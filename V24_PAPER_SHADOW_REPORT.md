# V24 PAPER / SHADOW REPORT

PAPER and SHADOW are explicit and cannot be replaced by LIVE.

The empirical dataset rejects any mode other than `PAPER` or `SHADOW`.

Every observation/decision is retained, including NO BET.

Real-money execution is disabled.

Fake-provider E2E verified:
provider-like payload → normalization → source freshness → consensus baseline → decision → immutable dataset → hash verification.
