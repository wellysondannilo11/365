# ROBO DA BET V16 — BENCHMARK REPORT

The benchmark framework is prepared for:

- market-only baseline;
- favorite baseline;
- Elo baseline;
- simple probabilistic baseline;
- Robo candidate models.

No empirical benchmark winner is declared because the supplied dataset is DEMO-only.
