# V19 — BENCHMARK REPORT

## Real-data benchmark

**NOT AVAILABLE.**

The required comparison remains:

1. market-only baseline;
2. naive/favorite baseline;
3. Elo/statistical baseline;
4. football-feature model;
5. ML model;
6. Robo fair-pricing/dislocation strategy.

No benchmark winner is declared without temporally held-out real observations.

## Software pricing benchmark

A controlled performance benchmark executed 1,000 pricing calls in approximately **8.08 seconds** with approximately **0.34 MB peak traced Python memory** for the loop. This is a software performance result only, not a profitability benchmark.
