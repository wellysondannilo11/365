# ROBO DA BET V16 — EXPERIMENTS REPORT

Each future empirical run should record dataset hash, feature version, model version, configuration, seed, markets, leagues, threshold, stake policy and holdout state.

The existing immutable experiment registry is preserved. V16's empirical runner adds dataset fingerprinting and explicit real-data evidence classification.

No model-selection claim is made from the DEMO fixture.
