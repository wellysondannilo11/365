This release uses a detached SHA-256 checksum for the ZIP artifact.
The authoritative checksum is the external V17_SHA256.txt delivered alongside the ZIP.
A ZIP cannot contain its own final checksum without becoming self-referential.
