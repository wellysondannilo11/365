# V24 DATASET REPORT

Dataset path:
`data/research/robo_bet_dataset_v24.jsonl`

Each row can carry:
event, snapshot, bookmaker, market, selection, odds, source timestamp, captured timestamp, live state, features/lineage, model/pricing versions, fair probability, fair odds, edge, EV, decision, mode, stake, result, P/L and CLV.

Each row has:
- dataset version;
- observation ID;
- previous hash;
- row hash.

Tampering is detectable through chain verification.

Current real-data count:
**0 real events / 0 real snapshots / 0 real decisions / 0 real paper bets**, because provider credentials were unavailable.

Synthetic/fake-provider tests are not included as empirical evidence.
