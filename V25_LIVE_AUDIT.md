# V25 Live Audit

V25 adds live snapshot quality validation and a live reprice endpoint using the supplied live state and expected-goal inputs to produce a point-in-time scoreline distribution.

Position management is independent from entry filtering and supports HOLD, REDUCE and EXIT. Reversal is evaluated as a new independent value calculation.

Real live-stat provider execution remains blocked because no real provider credentials/session were available.
