# FINAL LIVE AUDIT

Live snapshot validation rejects missing, future and stale source/capture timestamps and invalid minutes.

Live repricing incorporates the current score into final-score distribution and compares current markets independently.

The real live provider was not executed. The Odds API integration is prepared for odds/live-event observations; richer live statistics are only used when a provider actually supplies them.
