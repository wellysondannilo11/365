# V23 PAPER / SHADOW Report

Both modes are explicit and real-money execution is disabled. Operational polling is available through `/v22/session/poll` and `ml/scripts/run_v23_paper.py`. Real-feed execution is BLOCKED until a valid provider credential is supplied.
